*.log*
__pycache__/
*.pyc
.pytest_cache/
build/
dist/
*.spec.bak

# Multicast captures from tools/record_streams.py. Real ones are worth keeping,
# but deliberately, not by accident -- a night of packets is a large file.
capture.jsonl
*.capture.jsonl
